apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bookstore-api.fullname" . }}-test-connection"
  labels:
    {{- include "bookstore-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bookstore-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
